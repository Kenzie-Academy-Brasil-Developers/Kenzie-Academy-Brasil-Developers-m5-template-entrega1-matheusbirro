import { category, categoryCreate } from "./category.interfaces";
import { 
    Task, 
    TaskCreate, 
    TaskReturn, 
    TaskUpdate 
} from "./task.interfaces";


export { Task, TaskCreate, TaskUpdate, TaskReturn, category, categoryCreate}